# 🍒 น้องเชอร์รี่ 2D — แดนมอนสเตอร์

เกมแอ็กชันเลื่อนข้าง (side-scrolling) แนวเมเปิ้ลสตอรี่ ใช้ Phaser 3
เป็นเกมคนละตัวกับเกม 3D เดิม (`../index.html`) — เกมเดิมยังเล่นได้ตามปกติ ไม่ถูกแก้อะไรเลย

## วิธีเล่น

ต้องเปิดผ่านเว็บเซิร์ฟเวอร์ (เพราะใช้ ES module ซึ่งเปิดด้วย `file://` ตรง ๆ จะติด CORS)

```bash
npx http-server -p 8080 .      # รันที่โฟลเดอร์ราก repo
# แล้วเปิด http://localhost:8080/maple/
```

บน GitHub Pages เข้าที่ `https://<user>.github.io/<repo>/maple/` ได้เลย ไม่ต้อง build

## ปุ่ม

| ปุ่ม | ทำอะไร |
|---|---|
| `←` `→` หรือ `A` `D` | เดิน |
| `Space` | กระโดด |
| `↓` + `Space` | ทิ้งตัวลงชั้นล่าง |
| `↑` | จับเชือก/บันได · เข้าพอร์ทัล |
| `↑` `↓` (ตอนเกาะเชือก) | ไต่ขึ้น/ลง · `Space` กระโดดออก |
| `X` | โจมตีปกติ |
| `1` – `4` | สกิลของอาชีพ (ใช้มานา) |
| `Z` | เก็บของที่ตกพื้น |

บนมือถือมีปุ่มบนจอให้อัตโนมัติ (แป้นทิศทางซ้าย · กระโดด/โจมตี/เก็บของขวา · แตะช่องสกิลเพื่อใช้สกิล)

## กลไกที่ทำให้เป็นเกมแนวเมเปิ้ล

1. **Foothold ชนทางเดียว** — พื้นเป็นเส้นแท่นลอยซ้อนชั้น กระโดดทะลุขึ้นจากใต้แท่นได้
   แต่ยืนบนแท่นได้เมื่อตกลงมาจากด้านบน (ปิด `checkCollision` ด้านล่างและด้านข้าง)
2. **ทิ้งตัวลงชั้นล่าง** — `↓` + `Space` ปิดการชนเฉพาะแท่นระดับเดียวกับที่ยืนอยู่
   จนกว่าตัวจะร่วงพ้นแท่นนั้นจริง (วัดจากตำแหน่ง ไม่ใช่ตัวจับเวลา) แท่นชั้นถัดไปยังชนปกติ
   จึงไม่ร่วงทะลุสองชั้นในการกดครั้งเดียว และพื้นล่างสุดทิ้งตัวลงไม่ได้
3. **เชือก/บันได** — ปิดแรงโน้มถ่วง ล็อก x เข้ากลางเชือก ไต่ขึ้นลงด้วยความเร็วคงที่
   กระโดดออกกลางทางได้ ไต่ถึงยอดแล้วขึ้นไปยืนบนแท่นบนอัตโนมัติ
4. **โจมตีเป็นพื้นที่ ไม่ล็อกเป้า** — สกิลกินกรอบสี่เหลี่ยมหน้าตัว โดนมอนได้สูงสุด 8 ตัวพร้อมกัน
   สกิลที่มี `hits` หลายครั้งจะทยอยลงทีละ 90ms ทำให้ตัวเลขดาเมจเด้งซ้อนกันเป็นชุด
5. **ของตกพื้น เก็บด้วยมือ** — ของเด้งออกจากตัวมอนแล้วตกค้างบนแท่น ต้องเดินไปทับแล้วกด `Z`
   ของหายเองใน 45 วินาที (กะพริบเตือน 8 วินาทีสุดท้าย)
6. **มอนผูกกับแท่นของตัวเอง** — เดินวนไปกลับ ไม่ไล่ข้ามชั้น รีสปอว์น 4.2 วินาทีหลังตาย (ทีละตัว)

## โครงสร้างไฟล์

```
shared/gamedata.js        ตารางข้อมูลเกมกลาง (สกัดจาก cherry-adventure.jsx)
maple/
  index.html              เชลล์ + โหลด Phaser จาก CDN + แสดงข้อผิดพลาดบนจอ
  src/main.js             ตั้งค่า Phaser และลงทะเบียนฉาก
  src/maps.js             โครงแมพ: foothold / เชือก / พอร์ทัล / จุดสปอว์น
  src/rng.js              สุ่มแบบมี seed (แมพ procedural ต้องออกมาเหมือนเดิมทุกครั้ง)
  src/textures.js         สร้างกราฟิกทั้งหมดตอนรันไทม์ — ไม่มีไฟล์รูปเลย
  src/rig.js              ตัวละครแบบข้อต่อ ย้อมสีตามอาชีพ
  src/player.js           การเคลื่อนที่ (แท่น/เชือก/ทิ้งตัว/โจมตี/โดนตี)
  src/mob.js              มอนสเตอร์
  src/drop.js             ของที่ตกพื้น
  src/stats.js            สูตรดาเมจ · ค่าประสบการณ์ · การดรอปของ
  src/hud.js              แถบสถานะบนจอ
  src/input.js            รวมคีย์บอร์ด + ปุ่มจอสัมผัส
  src/fx.js               ตัวเลขดาเมจ · เอฟเฟกต์ฟัน · ประกาย
  src/save.js             localStorage (คีย์ `cherry-maple-save-v1` แยกจากเกม 3D)
  src/scenes/             BootScene · TitleScene · FieldScene
```

## ข้อมูลเกมใช้ร่วมกับเกม 3D

`shared/gamedata.js` สกัดมาจากส่วนหัวของ `cherry-adventure.jsx` (66 export)
เกม 2D ดึงค่าสมดุลจากไฟล์นี้ทั้งหมด:

- `SPECIES` มอน 14 ตัว → หน้ามอน (อีโมจิ) · เลือด · พลังโจมตี · tier (คุมขนาดตัว)
- `BIOMES` 6 ไบโอม → 6 แมพ พร้อมสีพื้น/ท้องฟ้า · ช่วงเลเวล · พูลมอน
- `CLASSES` 8 อาชีพ + `CLASS_OUTFIT` → สีชุดตัวละคร · `CLASS_SKILLS` → สกิลปุ่ม 1–4
- `LOOT` 132 ชิ้น + `rollRarity` / `rollQuality` / `PREFIXES` → ของดรอปมีคำนำหน้าคุณภาพเหมือนเกมเดิม
- `WEAK` + `SKILL_ELEM` → ตีเข้าจุดอ่อนธาตุ ดาเมจ ×1.5 ขึ้นป้าย `WEAK!`
- `MATERIALS` → วัตถุดิบคราฟต์ที่ดรอปจากมอน

**ข้อควรรู้:** ตอนนี้ยังมีข้อมูลสองชุด — `cherry-adventure.jsx` กับ `index.html`
ยังถือสำเนาของตัวเองอยู่ (ยังไม่ได้แก้ให้ `import` จาก `shared/`) เพราะเกม 3D เป็นไฟล์
เดียว 1.1MB + เวอร์ชัน precompiled 1.2MB การรื้อให้ใช้ module ร่วมกันเป็นงานแยกที่ต้อง
ทดสอบเกมเดิมใหม่ทั้งหมด ถ้าจะแก้ตัวเลขสมดุลตอนนี้ **ต้องแก้ทั้งสองที่**

## สิ่งที่ยังไม่มี

- บอสประจำแมพ (ข้อมูล `boss` / `bossName` มีอยู่ใน `BIOMES` แล้ว รอเอามาใช้)
- หน้ากระเป๋า/สวมของ (ตอนนี้เก็บของแล้วสวมอัตโนมัติถ้าแรงกว่าของเดิม)
- เตาหลอม (คราฟต์/ตีบวก/ฝังเจม) — `CRAFT_RECIPES` · `GEMS` · `ELEM_INFUSE` พร้อมใช้แล้ว
- สายอาชีพที่ Lv.40 (`CLASS_PATHS`) · ท่าไม้ตาย (`ULTS`) · ต้นสกิล (`SKILL_TREE`)
- ระบบเควส · NPC · เมือง · PvP
